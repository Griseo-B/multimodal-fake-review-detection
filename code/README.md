# 代码说明

| 目录 | 用途 |
|------|------|
| `data/` | 读取清单、清洗、划分 train/val/test |
| `models/` | 模型定义 |
| `train/` | 训练脚本 |
| `eval/` | 评估与出结果表 |
| `explain/` | 特征贡献与模板化解释 |

## 约定

- 随机种子固定，保证可复现  
- 每次正式实验在 `experiments/` 新建一条记录  
- 大数据路径写在配置文件或环境变量，不要写死含个人信息的绝对路径到公开 fork  

当前为仓库骨架，训练代码后续按周迭代加入。
